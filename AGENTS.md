# AI Tool Entry

Read `.ai/RULES.md` and `.ai/STATE.md` before working, then `CLAUDE.md`.
The shared project rules live in `.ai/RULES.md`; current state in `.ai/STATE.md`.

Live static vet marketing site (branch `master`). Do not push/deploy, submit real `/contact` leads, change pricing/medical-service copy, modify secrets, or mutate production without the required gate (clear, scoped confirmation from David). `git push` to `master` = production deploy.
